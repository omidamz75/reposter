# راهنمای مشارکت در پروژه

## گردش کار گیت
1. برای هر قابلیت جدید، یک برنچ از `main` ایجاد کنید:
```bash
git checkout -b feature/نام-قابلیت
```

2. تغییرات را در برنچ جدید اعمال کنید:
```bash
git add .
git commit -m "توضیح تغییرات"
```

3. تست‌ها را اجرا کنید:
```bash
pytest tests/
```

4. پس از اطمینان از عملکرد، به برنچ اصلی مرج کنید:
```bash
git checkout main
git merge feature/نام-قابلیت
```

## استاندارد کدنویسی
- از Type Hints استفاده کنید
- برای هر تابع docstring بنویسید
- برای هر قابلیت جدید تست بنویسید
- از black برای فرمت کد استفاده کنید
